[project]
name = "thought-signature-practice"
version = "0.1.0"
description = "Thought Signature Practice project"
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = "^3.10"
dependencies = [
    "google-genai (>=1.0.0)",
    "supabase (>=2.0.0)",
    "python-dotenv (>=1.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
